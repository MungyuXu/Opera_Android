<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.my.opera">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <service android:name=".MusicServer">
            <intent-filter>
                <action android:name="com.angel.Android.MUSIC" />

                <category android:name="android.intent.category.default" />
            </intent-filter>
        </service>

        <activity android:name=".StartActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginActivity" />
        <!-- <activity android:name=".StartActivity" /> -->
        <activity android:name=".RegisterActivity" />
        <activity android:name=".DeleteActivity" />
        <activity android:name=".UpdateActivity" />
        <activity android:name=".AddOperaActivity" />
        <activity android:name=".DeleteOperaActivity" />
        <activity android:name=".UpdateOperaActivity" />
        <activity android:name=".OperaActivity" />
        <!-- <activity android:name=".fragment.Fragment6" /> -->
        <!-- <activity android:name=".fragment.Fragment6"></activity> -->
        <activity android:name=".ManageActivity" />
        <!-- MusicActivity   StartActivity -->
        <!-- <activity android:name=".StartActivity" /> -->
        <activity android:name=".MusicActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".RatActivity" />
        <activity android:name=".SecondActivity" />
        <activity android:name=".LzActivity" />
        <activity android:name=".SpinnerActivity"></activity>
    </application>

</manifest>